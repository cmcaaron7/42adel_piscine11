#include <unistd.h>

void ft_putchar(char c);

void rush
{
	while (int y = 1; y < height; y++)
	{
		while (int x = 1, x < width + 1, x++)
		{
			char c = ' ';
		}
			while (y == 1 || y == height)
			{
				c = '-';
			} c = '|';
	 ft_putchar(c);
	} ft_putchar('\n');
}
